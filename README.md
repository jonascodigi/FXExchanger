FXExchanger

FXExchanger is a console application for currency exchange.

Configuration

1. Open the file appsettings.json in the FXExchanger.ConsoleApp root folder.
2. Replace the <API_KEY_HERE> with the actual API KEY you received via email
3. Run the console application